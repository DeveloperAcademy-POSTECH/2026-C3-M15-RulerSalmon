Put bundled voice embedding files here.

Expected names:
- chaem.embedding.json
- cindy.embedding.json
- friday.embedding.json

Each file may be either a plain JSON float array or the JSON object printed by
`speech embed-speaker --json`.
